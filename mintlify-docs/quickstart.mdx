---
title: "Quickstart"
description: "Install CCC in under 60 seconds. Desktop plugin is the recommended path for v4.0.0-beta.11."
---

## Desktop Plugin (Recommended)

The fastest path to CCC v4.0.0-beta.11. Two commands inside any Claude Code session:

<Steps>
  <Step title="Add to marketplace">
    ```
    /plugin marketplace add KevinZai/commander
    ```
  </Step>
  <Step title="Install the plugin">
    ```
    /plugin install commander
    ```
  </Step>
  <Step title="Open the command center">
    ```
    /ccc
    ```
    Arrow keys to navigate. Enter to select. The full menu appears with skill recommendations based on your current project.
  </Step>
</Steps>

**What you get:** 55 plugin skills, 17 specialist sub-agents, 8 lifecycle hooks × 16 handlers, 2 bundled MCP servers. All free forever — no paid tier, no caps.

<Card title="Plugin docs" icon="puzzle-piece" href="/plugin/install">
  Full plugin installation guide — verification, first commands, and troubleshooting.
</Card>

---

## Advanced / Legacy Install Paths

These paths are for developers who want full CLI control or prefer staying inside the terminal.

<Tabs>
  <Tab title="CLI (npm)">
    You use `claude` in your terminal and want the full interactive CLI binary.

    ```bash
    npm install -g cc-commander
    ```

    Then launch the interactive CLI:

    ```bash
    ccc
    ```

    **What you get:** Full CLI with tmux split mode, daemon, theme switching, cockpit dashboard, and all 502+ skills.

    **Best for:** Developers who live in the terminal.
  </Tab>

  <Tab title="Slash Commands (curl)">
    You don't want a separate CLI binary. Just skills + commands inside Claude Code sessions.

    ```bash
    curl -fsSL https://raw.githubusercontent.com/KevinZai/commander/main/install-remote.sh | bash
    ```

    Then in any Claude Code session:

    ```
    /ccc
    ```

    Full interactive menu appears. Same features, no extra binary.

    **What you get:** All 502+ skills and commands accessible via `/ccc` prefix.

    **Best for:** Developers who prefer staying inside Claude Code.
  </Tab>
</Tabs>

## Your First 60 Seconds

```
1. /plugin install commander   → Plugin installs
2. /ccc                  → Main menu (55 skills)
3. Pick a skill          → CCC loads the right context
4. Session ends          → CCC auto-saves lessons
5. Next session          → CCC is smarter about your project
```

No YAML. No API keys. No configuration.

## Verify Your Install

```bash
ccc --test
```

Runs 22-point self-verification. All green means you're ready.

```bash
ccc --status
```

Health check JSON — shows gateway, skill count, session state.

## What's Next

<CardGroup cols={2}>
  <Card title="Plugin Skills" href="/plugin/skills" icon="wand-magic-sparkles">
    All 55 plugin skills — workflows, domain routers, deploy, fleet, and more.
  </Card>
  <Card title="Agents" href="/plugin/agents" icon="robot">
    17 specialist sub-agents for review, build, research, debug, fleet, and more.
  </Card>
  <Card title="MCP Integrations" href="/plugin/mcp-integrations" icon="plug">
    2 bundled + 16 opt-in MCP servers via /ccc-connect.
  </Card>
  <Card title="Codex Compatibility" href="/features/codex-compatibility" icon="code">
    Use CC Commander skills in OpenAI Codex CLI and Codex Desktop.
  </Card>
</CardGroup>
